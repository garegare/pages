<?xml version="1.0" encoding="UTF-8"?>
<rss xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:wfw="http://wellformedweb.org/CommentAPI/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:sy="http://purl.org/rss/1.0/modules/syndication/" xmlns:slash="http://purl.org/rss/1.0/modules/slash/" xmlns:webfeeds="http://webfeeds.org/rss/1.0" version="2.0">
  <channel>
    <link>https://garegare.github.io/pages/</link>
    <atom:link href="https://garegare.github.io/pages/feed.xml" rel="self" type="application/rss+xml"/>
    <lastBuildDate>Mon, 27 Apr 2026 01:43:47 GMT</lastBuildDate>
    <language>ja</language>
    <generator>Lume v2.5.2</generator>
    <item>
      <title>LLMを用いた生成システムにおける正規化設計プラクティス</title>
      <link>https://garegare.github.io/pages/posts/lm-normalization-design/</link>
      <guid isPermaLink="false">https://garegare.github.io/pages/posts/lm-normalization-design/</guid>
      <description>LLMベースの生成システムで重要になる「正規化レイヤー」の考え方と、設計原則、失敗パターン、実践的な導入手順を整理する</description>
      <content:encoded>
        <![CDATA[<h2>概要</h2>
<p>LLM（大規模言語モデル）を活用した生成システムは、高い柔軟性と表現力を持つ一方で、その出力は必ずしも安定していない。</p>
<p>入力・出力ともに構造が揺らぎやすく、そのままアプリケーションに組み込むと、さまざまな問題を引き起こす。</p>
<ul>
<li>出力フォーマットの揺れ</li>
<li>意図しない構造変化</li>
<li>downstream処理との不整合</li>
<li>再現性の低さ</li>
</ul>
<p>これらの問題に対処するために重要になるのが、「正規化レイヤー」という設計である。</p>
<p>本記事では、LLMベースの生成システムにおける正規化設計の基本パターンを整理する。</p>
<hr>
<h2>なぜ正規化が必要か</h2>
<p>LLMの出力は、本質的に以下の性質を持つ。</p>
<ul>
<li>自由度が高い</li>
<li>文脈に依存して揺れる</li>
<li>厳密な構造保証がない</li>
</ul>
<p>この特性により、実運用では次のような問題が発生する。</p>
<ul>
<li>JSONを期待しているのに自然文で返る</li>
<li>必須フィールドが欠落する</li>
<li>意味は近いが異なる値が出る</li>
<li>想定していないスキーマ構造になる</li>
</ul>
<p>したがって、</p>
<p>LLMの出力をそのまま使ってはいけない。</p>
<p>LLMの出力はあくまで「提案」であり、「確定したデータ」ではないという前提が必要になる。</p>
<hr>
<h2>基本アーキテクチャ</h2>
<p>LLMを利用した生成システムでは、以下のような三層構造を採用するのが効果的である。</p>
<pre><code class="language-text">[自由入力 / LLM出力]
        ↓
[中間表現（ゆるい構造）]
        ↓
[正規化（厳密構造）]
        ↓
[アプリケーションロジック]
</code></pre>
<p>自由な生成と、厳密な構造保証を分離することが重要なポイントになる。</p>
<hr>
<h2>中間表現の役割</h2>
<p>中間表現は、LLMの自由度を活かしつつ、後段で扱える形に受け止めるための層である。</p>
<p>ここでは次のような設計を行う。</p>
<ul>
<li>LLMには自由に生成させる</li>
<li>構造はあえて「緩く」定義する</li>
<li>欠損や表現の揺れを許容する</li>
</ul>
<p>例：</p>
<pre><code class="language-json">{
  &quot;role&quot;: &quot;調査員&quot;,
  &quot;entry&quot;: &quot;依頼を受けて訪れた&quot;,
  &quot;tone&quot;: &quot;ミステリー&quot;,
  &quot;scale&quot;: &quot;中くらい&quot;
}
</code></pre>
<p>この段階では、厳密なenumや型制約は不要である。意味が通ることを優先し、表現の揺れは許容する。</p>
<hr>
<h2>正規化レイヤーの役割</h2>
<p>正規化レイヤーでは、曖昧な中間表現を、厳密で扱いやすい構造へ変換する。</p>
<h3>値の正規化</h3>
<pre><code class="language-text">&quot;中くらい&quot; → Scale::Medium
&quot;medium&quot; → Scale::Medium
&quot;中規模&quot; → Scale::Medium
</code></pre>
<h3>構造の補完</h3>
<ul>
<li>欠損値をデフォルトで補う</li>
<li>不足フィールドを生成する</li>
</ul>
<h3>不正値の処理</h3>
<ul>
<li>無効な値を破棄</li>
<li>近い値にマッピング</li>
<li>fallbackを適用</li>
</ul>
<h3>型への変換</h3>
<pre><code class="language-rust">struct NormalizedParams {
    scale: Scale,
    complexity: Complexity,
    entry_type: EntryType,
}
</code></pre>
<hr>
<h2>設計原則</h2>
<h3>原則1：LLM出力を信頼しない</h3>
<p>LLMは「提案」であり「確定値」ではない。</p>
<h3>原則2：構造の最終責任はコード側に持たせる</h3>
<ul>
<li>enum</li>
<li>型</li>
<li>制約</li>
</ul>
<p>は必ずコード側で保証する。</p>
<h3>原則3：テキストを正本にしない</h3>
<p>自然文は解釈が揺れるため、</p>
<p>構造化データを正本とする。</p>
<h3>原則4：段階的に整形する</h3>
<p>一発で完全な構造を作ろうとしない。</p>
<ul>
<li>generate（自由）</li>
<li>normalize（制約）</li>
<li>refine（調整）</li>
</ul>
<p>に分離する。</p>
<h3>原則5：分類軸は固定、語彙は拡張</h3>
<ul>
<li>分類軸は固定</li>
<li>表現は拡張可能</li>
</ul>
<hr>
<h2>よくある失敗パターン</h2>
<h3>フル構造をLLMに生成させる</h3>
<ul>
<li>出力が巨大になる</li>
<li>破綻率が上がる</li>
</ul>
<h3>enumを直接生成させる</h3>
<ul>
<li>微妙に違う値が出る</li>
<li>validationが破綻する</li>
</ul>
<h3>テキストから毎回再解釈する</h3>
<ul>
<li>非決定的</li>
<li>パフォーマンス悪化</li>
</ul>
<h3>正規化を後付けする</h3>
<ul>
<li>設計が歪む</li>
<li>不整合が増える</li>
</ul>
<hr>
<h2>良いパターン</h2>
<h3>自由入力 → 正規化</h3>
<pre><code class="language-text">&quot;港町の事件を調べる旅人&quot;
        ↓
role = Traveler
entry_type = Observer
genre = Mystery
</code></pre>
<h3>生成と正規化の分離</h3>
<pre><code class="language-text">LLM → 中間JSON
Rust → 正規化
</code></pre>
<h3>組み合わせで表現力を出す</h3>
<pre><code class="language-rust">EntryType + ContactKind + Strength
</code></pre>
<hr>
<h2>応用領域</h2>
<p>この設計は以下に応用できる。</p>
<ul>
<li>ストーリー生成</li>
<li>NPC生成</li>
<li>ユーザープロファイル</li>
<li>ゲーム状態管理</li>
<li>対話システム</li>
<li>ワークフロー生成</li>
</ul>
<hr>
<h2>実践的な導入手順</h2>
<ol>
<li>LLMの出力を観察する</li>
<li>揺れパターンを収集する</li>
<li>中間表現を定義する</li>
<li>正規化ルールを書く</li>
<li>enum・型を設計する</li>
<li>fallback戦略を決める</li>
</ol>
<hr>
<h2>まとめ</h2>
<p>LLMを使ったシステムでは、</p>
<p>「生成」よりも「正規化」が重要になる。</p>
<p>本質は、</p>
<p>不安定な生成を、安定した構造に変換すること。</p>
<p>そのためには、</p>
<ul>
<li>自由生成と構造保証の分離</li>
<li>正規化レイヤーの明確化</li>
<li>型による制約</li>
</ul>
<p>が不可欠となる。</p>
]]>
      </content:encoded>
      <pubDate>Fri, 27 Mar 2026 00:00:00 GMT</pubDate>
    </item>
    <item>
      <title>ブログに RSS フィードを追加した</title>
      <link>https://garegare.github.io/pages/posts/rss-feed/</link>
      <guid isPermaLink="false">https://garegare.github.io/pages/posts/rss-feed/</guid>
      <description>Lume の feed プラグインを使って RSS 2.0 と JSON Feed を生成し、サイドバーにリンクを設置するまでの記録</description>
      <content:encoded>
        <![CDATA[<h2>やったこと</h2>
<p>このブログに RSS フィードを追加した。</p>
<ul>
<li><code>/feed.xml</code> — RSS 2.0 形式</li>
<li><code>/feed.json</code> — JSON Feed 形式</li>
</ul>
<p>あわせてサイドバーに RSS へのリンクと GitHub リポジトリへのリンクを設置した。</p>
<h2>Lume の feed プラグイン</h2>
<p>このブログは静的サイトジェネレーターの <a href="https://lume.land/">Lume</a> を使っている。Lume には公式の <code>feed</code> プラグインがあり、設定を追加するだけでフィードを生成できる。</p>
<p><code>_config.ts</code> に以下を追加した。</p>
<pre><code class="language-ts">import feed from &quot;lume/plugins/feed.ts&quot;;

site.use(feed({
  output: [&quot;/feed.xml&quot;, &quot;/feed.json&quot;],
  query: &quot;type=post&quot;,
  sort: &quot;date=desc&quot;,
  info: {
    title: &quot;=site.blog_title&quot;,
    description: &quot;=site.blog_description&quot;,
    lang: &quot;ja&quot;,
  },
  items: {
    title: &quot;=title&quot;,
    description: &quot;=description&quot;,
  },
}));
</code></pre>
<p><code>output</code> に複数のパスを指定することで、拡張子に応じて RSS 2.0 と JSON Feed の両方を同時に生成できる。<code>query</code> でフィードに含めるページを絞り込んでいる。</p>
<h2>ブラウザ自動検出</h2>
<p>RSS 対応ブラウザやリーダーがフィードを自動検出できるよう、<code>&lt;head&gt;</code> に <code>&lt;link rel=&quot;alternate&quot;&gt;</code> を追加した。</p>
<pre><code class="language-html">&lt;link rel=&quot;alternate&quot; type=&quot;application/rss+xml&quot; title=&quot;{{ site_name }}&quot; href=&quot;/feed.xml&quot;&gt;
</code></pre>
<p>これにより、ブラウザの拡張機能や一部のリーダーアプリが自動的にフィードを見つけられるようになる。</p>
<h2>サイドバーへのリンク設置</h2>
<p>自動検出だけでは気づきにくいので、サイドバーにもリンクを設置した。プロフィールエリアと検索・タグエリアの間に新しいセクションを追加している。</p>
<pre><code class="language-html">&lt;section class=&quot;sidebar-section&quot;&gt;
  &lt;ul class=&quot;sidebar-tag-list&quot;&gt;
    &lt;li&gt;&lt;a href=&quot;/feed.xml&quot;&gt;RSS&lt;/a&gt;&lt;/li&gt;
    &lt;li&gt;&lt;a href=&quot;https://github.com/garegare?tab=repositories&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;GitHub&lt;/a&gt;&lt;/li&gt;
  &lt;/ul&gt;
&lt;/section&gt;
</code></pre>
<p>ついでに GitHub のリポジトリ一覧へのリンクも並べて置いた。</p>
]]>
      </content:encoded>
      <pubDate>Sun, 15 Mar 2026 00:00:00 GMT</pubDate>
    </item>
    <item>
      <title>GitHub Issue リンクを正しく生成する Claude Code スキルを作った</title>
      <link>https://garegare.github.io/pages/posts/github-issue-link-skill/</link>
      <guid isPermaLink="false">https://garegare.github.io/pages/posts/github-issue-link-skill/</guid>
      <description>Claude Code のスキル機能を使って、日本語や改行を含む GitHub Issue 作成リンクを正しく生成できるようにするまでの記録</description>
      <content:encoded>
        <![CDATA[<h2>背景</h2>
<p>Claude Code でブログ作業をしていて、会話の流れを GitHub Issue としてワンタップで起票できると便利だと思い、Issue 作成リンクの生成を試みた。</p>
<p>ただ単純に実装しようとすると、いくつか問題にぶつかった。</p>
<p>なお、今回の作業は <strong>Claude Code web 版</strong>で行っている。ローカルの開発環境はなく <code>gh</code> コマンドも使えない環境なので、PR 作成もリンクを貼る形で対応している。</p>
<h2>問題1: マークダウンリンクでの二重エンコード</h2>
<p>GitHub の Issue 作成 URL は以下のような形式で、<code>title</code> と <code>body</code> をクエリパラメータとして渡せる。</p>
<pre><code>https://github.com/owner/repo/issues/new?title=タイトル&amp;body=本文
</code></pre>
<p>最初に試したのは、マークダウンのリンク形式でこの URL を書く方法。</p>
<pre><code class="language-markdown">[Issueを作成する](https://github.com/.../issues/new?title=...&amp;body=...)
</code></pre>
<p>ところが、本文に改行（<code>%0A</code>）や <code>##</code> などの記号を含めると、レンダラーが <code>%</code> を再度エンコードしてしまい、GitHub 側で正しく解釈されなかった。</p>
<h2>問題2: HTML タグはタップで開けない</h2>
<p>次に <code>&lt;a&gt;</code> タグを試したが、Claude Code のチャット UI 上ではタップしてもリンクが開かない。これも実用的ではなかった。</p>
<h2>解決策: 完全 URL エンコード</h2>
<p><strong>title と body のパラメータ値をあらかじめ完全に URL エンコードしておく</strong>ことで、二重エンコードの問題を回避できた。</p>
<p>Python3 の <code>urllib.parse.quote</code> を使って、日本語・記号・改行を含むすべての文字を <code>%XX</code> 形式に変換する。</p>
<pre><code class="language-bash">python3 -c &quot;import urllib.parse; print(urllib.parse.quote('## 概要\n内容をここに書く', safe=''))&quot;
# → %23%23%20%E6%A6%82%E8%A6%81%0A%E5%86%85%E5%AE%B9%E3%82%92%E3%81%93%E3%81%93%E3%81%AB%E6%9B%B8%E3%81%8F
</code></pre>
<p><code>safe=''</code> を指定することで <code>/</code> なども含めてすべての文字がエンコードされる。また、<code>+</code> による省略エンコードは使っていない（ブラウザによっては正しく扱われないケースがあるため）。</p>
<p>生成されるリンクはこういう形になる。</p>
<pre><code class="language-markdown">[新しいIssueを作成する](https://github.com/owner/repo/issues/new?title=%E3%82%BF%E3%82%A4%E3%83%88%E3%83%AB&amp;body=%E6%9C%AC%E6%96%87)
</code></pre>
<h2>Claude Code スキルとして登録</h2>
<p>毎回手動でエンコードするのは面倒なので、この手順を Claude Code のスキルとして登録した。</p>
<p>スキルは <code>~/.claude/skills/&lt;スキル名&gt;/SKILL.md</code> に置くだけで機能する。</p>
<pre><code>~/.claude/skills/github-issue-link/SKILL.md
</code></pre>
<p>内容はこんな感じ。</p>
<pre><code class="language-markdown">---
name: github-issue-link
description: GitHubのIssue作成リンクを生成する。タイトルと本文を完全URLエンコードしたマークダウンリンクを出力する。
---

# GitHub Issue リンク生成スキル

## ルール
- title と body のパラメータ値は完全URLエンコードする
- 改行は %0A、スペースは %20、コロンは %3A、シャープは %23
- + による省略エンコードは使わない（ブラウザ互換性のため）
- リポジトリURLは git remote get-url origin から取得する

## URLエンコード手順
python3 -c &quot;import urllib.parse; print(urllib.parse.quote('文字列', safe=''))&quot;

## 手順
1. git remote get-url origin でリポジトリURLを取得
2. title・body を python3 でURLエンコード
3. マークダウンリンクを組み立てて出力
</code></pre>
<p>登録後は「IssueリンクをつくってくれやGitHubに報告したい」などと伝えるだけで、Claude が自動的にこのスキルを使ってリンクを生成してくれる。</p>
<h2>まとめ</h2>
<table>
<thead>
<tr>
<th>方法</th>
<th>結果</th>
</tr>
</thead>
<tbody>
<tr>
<td>マークダウンリンク（未エンコード）</td>
<td><code>%</code> が二重エンコードされ壊れる</td>
</tr>
<tr>
<td>HTML <code>&lt;a&gt;</code> タグ</td>
<td>タップで開けない</td>
</tr>
<tr>
<td>完全 URL エンコード</td>
<td>正常動作</td>
</tr>
</tbody>
</table>
<p>URL エンコードは一見シンプルに見えるが、レンダラーの挙動によって二重エンコードが起きるケースがある。GitHub Issue のプリフィル URL を扱う際は、<code>urllib.parse.quote</code> で完全にエンコードしておくのが確実。</p>
<p>Claude Code のスキル機能は <code>~/.claude/skills/</code> に SKILL.md を置くだけで有効になるので、手順を定型化したい場面では積極的に使っていきたい。</p>
]]>
      </content:encoded>
      <pubDate>Wed, 11 Mar 2026 00:00:00 GMT</pubDate>
    </item>
    <item>
      <title>はじめての記事</title>
      <link>https://garegare.github.io/pages/posts/hello-world/</link>
      <guid isPermaLink="false">https://garegare.github.io/pages/posts/hello-world/</guid>
      <description>ブログ開設のご挨拶</description>
      <content:encoded>
        <![CDATA[<p>こんにちは！このブログへようこそ。</p>
<p><img src="https://garegare.github.io/pages/posts/hello-world/hello-world.svg" alt="ブログ開設"></p>
<p>このブログは <a href="https://lume.land/">Lume</a> を使って構築した静的サイトです。Lumeは Deno 上で動作する高速な静的サイトジェネレーターで、Nunjucks や Markdown など複数のテンプレートエンジンに対応しています。</p>
<h2>なぜブログを始めたか</h2>
<p>日々の学びや気づきをアウトプットする場所として、このブログを立ち上げました。技術的なメモや日常の出来事など、さまざまなテーマで記事を書いていく予定です。</p>
<h2>今後の予定</h2>
<ul>
<li>技術的な記事（Deno、Web開発など）</li>
<li>読書メモや気になったトピック</li>
<li>日常の出来事や雑記</li>
</ul>
<p>これからどうぞよろしくお願いします！</p>
]]>
      </content:encoded>
      <pubDate>Sat, 07 Mar 2026 00:00:00 GMT</pubDate>
    </item>
  </channel>
</rss>